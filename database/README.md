# Database

Contains SQLite schema files, connection helpers, migrations, and local database files for persisted dashboard data.

Database files such as `.db`, `.sqlite`, and `.sqlite3` are ignored by Git by default.
