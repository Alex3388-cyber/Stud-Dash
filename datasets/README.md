# Datasets

Stores data used by the dashboard and machine learning pipeline.

- `raw/`: original source datasets that should not be manually modified.
- `processed/`: cleaned, validated, and feature-engineered datasets ready for analysis or modeling.
