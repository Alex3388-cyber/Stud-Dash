# Models

Contains model training code, prediction helpers, and serialized model artifacts.

- `train_model.py`: starter training workflow using Scikit-learn.
- `predictor.py`: reusable prediction interface for Streamlit pages.
- `artifacts/`: saved models, encoders, metrics, and pipeline files.
