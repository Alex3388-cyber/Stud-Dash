# Pages

Reserved for future Streamlit page modules.

The current dashboard navigation is managed inside `app.py` so the sidebar can present a single clean menu:
Home, Upload Dataset, Data Exploration, Prediction, Clustering, and Reports.
