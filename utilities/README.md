# Utilities

Stores shared constants, helper functions, and small cross-cutting utilities used by multiple modules.

Avoid putting feature-specific business logic here when it belongs in `preprocessing/`, `models/`, or `visualizations/`.
